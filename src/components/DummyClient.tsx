"use client";

export default function DummyClient() {
  // This component exists solely to create a client reference manifest for
  // the parent page. It renders nothing and has no runtime behavior.
  return null;
}
